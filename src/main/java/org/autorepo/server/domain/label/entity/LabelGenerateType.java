package org.autorepo.server.domain.label.entity;

public enum LabelGenerateType {
    CSV,          // CSV로 생성
    EMOJI,        // 이모지 라벨 생성
    TEXT,         // 텍스트 라벨 생성
    CUSTOM        // 처음 부터 생성
}
